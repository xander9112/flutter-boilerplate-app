enum DashboardMenu { home, orders, packages, support, profile }

class RoutePath {
  static const String initial = '/';
  static const String auth = '/auth';
  static const String onboarding = '/onboarding';
  static const String main = '/main';
}
